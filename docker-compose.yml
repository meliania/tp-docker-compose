version: '3'
services:
  node:
    image: abdmel/tp-docker-compose
    ports:
      - 2300:80
    environment:
      - mongo_connection=mongodb://mongo:27017
    networks:
      - network
  mongo:
    image: mongo
    networks:
      - network
    volumes:
      - db:/data/db

networks:
  network:
    driver: bridge
volumes:
  db:
